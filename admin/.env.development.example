# Copy to .env.development for local admin without login (API must use DISABLE_AUTH=true).
VITE_DISABLE_AUTH=true

# Optional — only if you use @supabase/supabase-js
# VITE_SUPABASE_URL="https://YOUR_PROJECT.supabase.co"
# VITE_SUPABASE_ANON_KEY="your-publishable-or-anon-key"
