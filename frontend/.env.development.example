# Copy to .env.development for local dev (no login when API uses DISABLE_AUTH=true + seed).
VITE_DISABLE_AUTH=true

# Optional — only if you use @supabase/supabase-js in the app
# VITE_SUPABASE_URL="https://YOUR_PROJECT.supabase.co"
# VITE_SUPABASE_ANON_KEY="your-publishable-or-anon-key"
